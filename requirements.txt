pywinauto>=0.6.8 ; sys_platform == "win32"
Pillow>=10.0        ; sys_platform == "win32"
rapidocr-onnxruntime>=1.3   ; sys_platform == "win32"   # local OCR fallback for the UIA-invisible grids

# tools/make_pdf.py only -- renders DESIGN.md to the PDF Part 1 asks for.
# Not needed to run the automation.
markdown>=3.5 ; sys_platform != "win32"
weasyprint>=60 ; sys_platform != "win32"
